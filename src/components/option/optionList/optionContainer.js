import Option from '.';

const OptionContainer = ({ option }) => {
  const onActiveItem = () => {};
  return <Option option={option} onActiveItem={onActiveItem} />;
};

export default OptionContainer;
